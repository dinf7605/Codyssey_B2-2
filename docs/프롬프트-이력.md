# 요약 프롬프트 개선 이력

담당: C. PRD 11장이 요구하는 산출물은 **"초안 → 수정 → 최종" 이력**이다.
결과 프롬프트만 내면 감점 요인이다. **왜 바꿨는지**가 핵심이다.

기록 규칙: 프롬프트를 고칠 때마다 아래 표에 한 줄을 추가하고,
전문은 `prototype/summarize.py`의 `PROMPT_TEMPLATE`에 반영한다.

---

## v0.1 — 초안 (PRD 작성 시점)

```
너는 기술 뉴스를 요약하는 편집자야.
아래 기사를 정확히 3줄 이내로 요약해줘.

규칙:
- 각 줄은 "- "로 시작하는 불릿 형태
- 각 줄 60자 이내
- 사실만 전달하고 추측이나 의견을 넣지 말 것
- 기사에 없는 내용을 만들어내지 말 것
- 요약 외의 머리말, 맺음말, 설명은 출력하지 말 것

제목: {{title}}
본문: {{content}}
```

**설계 의도**

| 문장 | 왜 넣었나 |
|---|---|
| "너는 기술 뉴스를 요약하는 편집자야" | 역할 부여. 문체가 뉴스 요약체로 안정된다 |
| "정확히 3줄 이내" | FR-05 출력 형식 제약 |
| `- ` 불릿 지정 | Notion Text에 그대로 넣어도 읽히는 형태 |
| "사실만 / 만들어내지 말 것" | 환각 억제. 요약문이 원문과 어긋나면 S2 검수에서 걸린다 |
| "머리말, 맺음말 금지" | "다음은 요약입니다:" 같은 군더더기가 Notion에 저장되는 것을 막는다 |

---

## v0.2 — (M3에서 실측 후 작성)

> 아래는 **채워 넣을 자리**다. 실제로 10건 정도 돌려 보고 어긋난 사례를 적는다.

| 항목 | 내용 |
|---|---|
| 관측된 문제 | (예: 본문이 짧을 때 3줄을 채우려고 없는 내용을 지어냄) |
| 수정 내용 | (예: "내용이 부족하면 2줄로 끝내도 된다" 추가) |
| 수정 근거 | |
| 재검증 결과 | (몇 건 중 몇 건 개선) |

**검증 방법** — Gemini 호출 없이 형식 검증만 하려면:

```bash
cd prototype
python -c "import summarize; print(summarize.build_prompt({'title':'제목','description':'본문'}))"
```

실제 호출까지 하려면 (하루 몇 건만, R5 유의):

```bash
GEMINI_API_KEY=... python pipeline.py --dry-run --fixture fixtures/sample_rss.xml
```

---

## v1.0 — 최종 (M5 확정 시 기록)

```
(확정본 전문)
```

---

## 부록: 보너스 B2를 붙이는 경우

PRD 15장이 못박은 대로 **별도 호출을 추가하지 말고 기존 요약 호출을 확장**한다.
"기사 1건당 1회" 원칙(NFR-04)을 지키면서 보너스를 얻는 방법이다.

```
... (v1.0 규칙 그대로) ...

출력은 아래 JSON 형식만 출력할 것. 코드블록 표시(```)도 붙이지 말 것.
{"summary": ["줄1", "줄2", "줄3"], "sentiment": "긍정" | "중립" | "부정"}

제목: {{title}}
본문: {{content}}
```

- Make에서는 `{{parseJSON(...)}}`로 파싱 후 `summary`와 `sentiment`를 각 속성에 매핑한다.
- 모델이 ` ```json `으로 감싸는 경우가 잦으므로 `replace()`로 백틱을 먼저 제거한다.
- 참조 구현에 반영하려면 `summarize.postprocess()`를 JSON 파싱 버전으로 교체하고,
  파싱 실패 시 기존 텍스트 후처리로 **fallback**시킨다 (형식 하나 때문에 저장을 잃지 않게).
