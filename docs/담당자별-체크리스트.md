# 담당자별 체크리스트

PRD 11장의 협업 리스크(R6): **A가 다 만들고 나머지는 구경하는 구조**가 되기 쉽다.
막는 방법은 하나뿐이다 — **각자 자기 모듈을 직접 만들고, A는 연결만 한다.**
성공 기준 S6(전원이 자기 담당을 설명 가능)은 발표 때 실제로 검증된다.

---

## A — 팀장 / 워크플로우 설계

- [ ] Make 계정 생성, 팀원 초대 (무료 플랜 좌석 확인)
- [ ] **프로필 타임존 `Asia/Seoul` 설정** ← 안 하면 09:00이 안 지켜진다
- [ ] 시나리오 생성, Sequential processing 켜기
- [ ] [1] Schedule 모듈 구성 (매일 09:00)
- [ ] B/C/D가 만든 모듈을 하나의 시나리오로 연결
- [ ] [4], [8] Array aggregator 배치 (0건일 때 흐름 끊김 방지)
- [ ] 실행 1회당 실제 크레딧 소모량 측정 → `docs/make-시나리오-명세.md` 2절 표 갱신
- [ ] **결제(billing) 활성화 권한을 본인만 보유** (R3)
- [ ] M6: 7일 연속 무인 실행 로그 확보 ← **마지막 주에 시작하면 늦는다**

## B — 데이터 수집

- [ ] RSS 후보 3개의 주소가 **실제로 열리는지** 확인 (PRD 157줄 확인 항목)
- [ ] `description` 길이가 요약에 충분한지 확인 (한두 줄만 오는 피드는 요약 품질이 안 나온다)
- [ ] `pubDate` 포맷 확인 → `prototype/tests/test_date_utils.py`에 케이스 추가
- [ ] `guid` 제공 여부 확인 ← 없으면 링크 해시로 가야 하고 중복 위험이 올라간다
- [ ] 최종 피드 1개 + 백업 1개 확정 → `prototype/config.py`의 `FEEDS` 갱신 (미결 O1)
- [ ] [2] RSS 모듈 구성 (Max 20건)
- [ ] 선정 근거 문서화 (README에 들어간다)

> 실제 피드를 확인하는 가장 빠른 방법:
> ```bash
> cd prototype && python -c "import rss,json; print(json.dumps(rss.load('피드주소','테스트',3), ensure_ascii=False, indent=2))"
> ```

## C — AI 요약

- [ ] Google AI Studio에서 API 키 발급 (신용카드 등록하지 말 것)
- [ ] **결제 비활성 상태 확인** — 켜는 순간 무료 티어가 사라진다 (PRD 8.2)
- [ ] 착수 시점의 실제 무료 한도 확인 → 모델 확정 (미결 O2, Flash 계열)
- [ ] `prototype/config.py`의 `GEMINI_MODEL` 갱신
- [ ] [10] HTTP 모듈 + [11] 후처리 변수 구성
- [ ] 실제 기사 10건으로 요약 품질 확인 → `docs/프롬프트-이력.md` v0.2 작성
- [ ] 3줄 초과 응답이 후처리로 잘리는지 확인 (E-07)
- [ ] 프롬프트 초안→수정→최종 이력 완성

## D — 저장 / 중복 방지

- [ ] Notion DB `기술 뉴스 아카이브` 생성, 속성 9개 정의
- [ ] 통합 생성 + **DB 페이지에 통합 연결** ← 빼먹으면 404
- [ ] Database ID 확보, Make Connection 등록
- [ ] [7]~[9] 중복 조회 + 필터 구성
- [ ] [12] Create a Database Item 매핑
- [ ] **중복방지키 생성 규칙을 확정하고 이후 절대 변경하지 않기**
- [ ] 같은 기사로 2회 실행 → 행 1건만 생기는지 확인 (S3)
- [ ] 발행일시가 KST로 들어가는지 확인 ← GMT 피드면 9시간 밀린다

## E — 품질 / 문서

- [ ] `docs/에러-테스트-케이스.md` 10건 실행 및 결과 기록
- [ ] 필수 4종(E-01/03/06/09) 스크린샷 확보
- [ ] 테스트 후 설정 원복 + 쓰레기 행 삭제
- [ ] README 총괄 (아래 항목 전부 포함되어야 함)
  - [ ] 팀 역할 / 개인별 작업 요약
  - [ ] 키워드 목록 + **선택 이유**
  - [ ] 에러 처리 정책 + **선택 이유**
  - [ ] 사용 툴명 (Make, Google Gemini API, Notion)
- [ ] **모든 스크린샷 API 키 마스킹 전수 검수** (R7, NFR-07)
- [ ] 제출물 체크리스트 (PRD 14.2) 최종 확인

---

## 주 2회 상호 설명 세션 (R6 대응)

각자 3분씩, 자기 모듈에 대해 답할 수 있어야 한다.

1. 내 모듈의 입력과 출력은 무엇인가
2. 실패하면 어떻게 되는가 (해당 에러코드)
3. 왜 이 방식을 골랐는가 (대안과 비교해서)

3번을 답하지 못하면 그 모듈은 아직 자기 것이 아니다.
