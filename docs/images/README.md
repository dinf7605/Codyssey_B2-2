# 제출용 스크린샷

루트 `README.md`가 이 폴더의 파일을 참조한다. **파일명이 정확히 일치해야** 이미지가 뜬다.

## 저장할 파일 목록

| 파일명 | 무엇을 찍나 | 확인 포인트 |
|---|---|---|
| `01-워크플로우-전체.png` | Make 캔버스 전체 (모듈 **11개** + 에러 핸들러 3개) | 왼쪽 RSS부터 오른쪽 Notion까지 다 보이게. `Retry 19` / `Resume 16` / `Retry 18`이 함께 나오면 좋다 |
| `02-notion-스키마.png` | Notion DB 헤더 (속성 9개) | 열 이름이 읽히게 |
| `03-저장-결과.png` | Notion에 저장된 행 | 요약문 3줄이 보이게 |
| `04-중복-스킵.png` | 2회차 실행 캔버스 | **`중복 아님 ⓧ0`** 과 **HTTP 9에 배지가 없는 것**이 같이 보여야 한다. S3·S4의 유일한 증거 |
| `05-E03-매칭0건.png` | T-03 실행 결과 | `주제매칭 ⓧ0`, RSS가 초록색(에러 아님) |
| `06-E01-RSS실패.png` | T-01 실행 결과 + 로그 | `Retry 19`와 로그 문구가 같이 |
| `07-E06-요약실패.png` | T-06 후 Notion 행 | 요약문 칸의 `요약 실패`, 나머지 칸이 채워진 것 |
| `08-E09-저장실패.png` | T-09 실행 결과 + 로그 | `Retry 18`, `[400] body failed validation` |
| `09-gemini-한도.png` | AI Studio > 비율 제한 | RPD 20이 보이게 |
| `10-스케줄.png` | Make 하단 바 | `Daily at 9:00 AM` + **토글이 켜진 상태** |
| `11-실행기록-7일.png` | Make → History | 09:00 실행이 7일치 쌓인 목록. **S1의 유일한 증거** |

## ⚠️ 재촬영 필요 (2026-08-03 구조 변경)

`Array aggregator 8`을 제거해 모듈이 12개 → **11개**가 됐다.
아래 3장은 제거 전 화면이라 README의 모듈 표와 어긋난다. **다시 찍을 것.**

| 파일 | 왜 |
|---|---|
| `01-워크플로우-전체.png` | 없는 모듈이 찍혀 있다 |
| `04-중복-스킵.png` | 구조가 다르고, 중복 차단이 실제로 동작하지 않던 시점의 화면이다 |
| `10-스케줄.png` | 01과 같은 캔버스 |

`04`는 특히 중요하다. **1회차 저장 성공 → 2회차 차단**을 두 장으로 남기면 더 좋다.

## 🚨 저장 전 확인

- [ ] **API 키·토큰이 화면에 없는가** — 있으면 가리고 다시 찍는다 (NFR-07, R7)
- [ ] Notion Database ID, Gemini 키, Notion Secret이 주소창이나 필드에 노출되지 않았는가
- [ ] 브라우저 북마크바에 개인 정보가 없는가 (F11 전체화면으로 찍으면 깔끔하다)

한 번 커밋하면 **파일을 지워도 git 히스토리에 남는다.** 커밋 전에 확인할 것.

## 캡처 요령

- 캔버스는 **축소(`Ctrl` + 마우스휠)** 해서 모듈이 전부 들어오게
- 로그가 필요한 컷은 하단 Logs 패널을 펼친 채로
- Windows: `Win` + `Shift` + `S` → 영역 선택
